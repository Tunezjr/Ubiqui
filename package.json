{
  "name": "ubiqui-monad-bot",
  "version": "0.2.0",
  "description": "Ubiqui — Monad Telegram trading bot on Vercel (webhook + cron).",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc -p . --noEmit",
    "lint": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "viem": "^2.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.4",
    "vercel": "^37.0.0"
  }
}
